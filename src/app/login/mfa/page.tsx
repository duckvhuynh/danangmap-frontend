import Link from "next/link";
import { IconArrowLeft, IconShieldLock } from "@tabler/icons-react";
import { BrandMark } from "@/components/brand-mark";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";

export default function MfaPage() { return <main className="grid min-h-[100dvh] place-items-center bg-surface-subtle p-5"><section className="w-full max-w-sm rounded-panel border bg-surface p-6 map-panel-shadow"><BrandMark/><span className="mt-8 grid size-11 place-items-center rounded-map-control bg-accent-subtle text-primary"><IconShieldLock stroke={1.75}/></span><h1 className="mt-5 text-xl font-semibold">Xác thực hai bước</h1><p className="mt-2 text-sm leading-6 text-muted-foreground">Nhập mã 6 chữ số từ ứng dụng xác thực của bạn.</p><form className="mt-6"><label htmlFor="mfa-code" className="mb-2 block text-sm font-medium">Mã xác thực</label><Input id="mfa-code" disabled title="Endpoint xác thực MFA chưa được kết nối" inputMode="numeric" autoComplete="one-time-code" minLength={6} maxLength={6} placeholder="000 000" className="text-center text-lg tracking-[0.25em]"/><Button disabled title="Endpoint xác thực MFA chưa được kết nối" className="mt-4 w-full">Xác nhận</Button></form><Button asChild variant="ghost" className="mt-6 -ml-3"><Link href="/login"><IconArrowLeft stroke={1.75}/>Quay lại đăng nhập</Link></Button></section></main>; }
